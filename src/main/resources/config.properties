seleniumGrid=false
gridURL=http://localhost:4444/wd/hub

#Application URL
url_base=http://localhost:8081/
url_local=http://localhost:8081/web/index.php/auth/login
url_grid=http://host.docker.internal/orangehrm/web/index.php/auth/login

#Browser Configuration
browser=chrome

#Credentials
username=Admin
password=Str0ng@Pass1

#ImplicitWait (in seconds)
implicitWait=30

#ExplicitWait (in seconds)
explicitWait=45

#Database (used by DBVerificationTest). Confirmed via:
#docker exec -it orangehrm-mysql mysql -u root -p (empty password worked),
#SELECT user, host FROM mysql.user -> only root@% and root@localhost exist,
#SHOW DATABASES -> orangehrm is present.
db.url=jdbc:mysql://localhost:3306/orangehrm
db.username=root
db.password=